{{- range $key, $value := .Values.deployments }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $key }}
  labels:
    app: {{ $key }}
spec:
  replicas: {{ $value.replicas | default $.Values.global.replicaCount }}
  selector:
    matchLabels:
      app: {{ $key }}
  template:
    metadata:
      labels:
        app: {{ $key }}
    spec:
      containers:
        - name: {{ $key }}
          {{- if hasPrefix "/" $value.image.repository }}
          image: "{{ $.Values.global.repository }}{{ $value.image.repository }}:{{ $value.image.tag | default "1.0.1" }}"
          {{- else }}
          image: "{{ $value.image.repository }}:{{ $value.image.tag | default "1.0.1" }}"
          {{- end}}
          imagePullPolicy: "IfNotPresent"
          {{- if not (eq $value.environment "disable") }}
          env:
            {{- range $envkey, $envvalue := $.Values.global.env }}
            - name: {{ $envvalue.name }}
              value: {{ $envvalue.value }}
            {{- end}}
          {{- end }}
      {{- if $value.volume}}
          volumeMounts:
            - mountPath: {{ $value.volume.mountPath }}
              name: {{ $key }}-0
      volumes:
        - name: {{ $key }}-0
          persistentVolumeClaim: 
            claimName: mongodb-pv-claim
      {{- end }}
{{- end }}